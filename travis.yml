language: php

php:
  - 7.0
  - 7.1

before_install:
  - sudo apt-get update
  - travis_retry composer self-update

install:
  - travis_retry composer update

script:
  - composer test

branches:
  only:
    - master
